# Perl.STD-SUBs --

    #PROMPT displayString defaultValue 
    sub prompt <<>> 
        else             <<>> 
        chomp($r=); 
        if ($r eq "") <<>> 
        return $r; 
    } 
     
     
