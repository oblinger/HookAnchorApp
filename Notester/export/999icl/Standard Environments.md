# 999icl.Standard Environments --

    NULL      -- The empty environment 
    MACHINE   -- The machine specific parameters: processor type, etc. 
    CORE      -- MACHINE + core parameters: ip address, etc. 
    PORTABLE  -- ???? parms  
    SAFE      -- CORE + minimal drivers & software 
    INSTALL   -- SAFE + related software 
    DEFAULT   -- SAFE + software 
     
