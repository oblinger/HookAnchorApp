# Docs.STRING\_SERACH\_CMD --


    Searches the current note for a specified string.  TAB-S-S will invoke the string search, and it will also 
    iteratively search for the next occurance of the string.  (Search is case insensitive.)
