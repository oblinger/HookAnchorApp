# NH.Trees --


       | URI             | [L]  Languages
       | URL             |
       |


     BACKSPACE  -- undo last keystroke
     ESC        -- cancel operation
     ENTER      -- Accept string input as typed so far
     TAB        -- Accept first listed string
     '['        -- Accept first listed string
     ']'        -- Accept current menu node
     ',' or '<' -- Build new menu tree from first listed string          (Clears input buffer)
     '.' or '>' -- Build new menu list of strings from subset of tree    (Clears input buffer)

    OPTIONS: 
     - Require 'ENTER' on menu commands
     - Don't show strings, Dont show menu

     ',' or '<' -- Build new 
