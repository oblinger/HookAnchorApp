# E\_Lisp -- Emacs lisp

    see also elisp 
     
    ENVIRONMENT: 
    (boundp 'running-xemacs)  
     
     ( 
    FILE:  
     ADD-SLASH:file-name-as-directory, DEL-SLASH:directory-file-name  
     PARENT:file-name-directory  
    file-name-non-directory 
    file-exists-p, expand-file-name  
     
    <<>> 
     
     
    split-string str [delims] 
    copy-file src dest ok-if-exist 
    string-match substr string 
     
    CONTROL 
     IF, WHILE, 
     SAVE-EXCURSION 
     
    I/O 
     INTERACTIVE 
     MESSAGE string 
     PRINC expr 
     READ-FROM-MINIBUFFER 
     
     
     
    BUFFER 
     current-buffer, buffer-file-name, buffer-modified-p 
     
    POINT 
     (goto-char (point))  (point-min) (point-max) 
     
     
    FILE 
     with-temp-buffer, insert-file-contents 
     
     
    MISC 
     STRING-MATCH pattern string   (returns boolean & more)  
     sit-for, sleep-for 

    see also elisp 
     
    ENVIRONMENT: 
    (boundp 'running-xemacs)  
     
     ( 
    FILE:  
     ADD-SLASH:file-name-as-directory, DEL-SLASH:directory-file-name  
     PARENT:file-name-directory  
    file-name-non-directory 
    file-exists-p, expand-file-name  
     
    <<>> 
     
     
    split-string str [delims] 
    copy-file src dest ok-if-exist 
    string-match substr string 
     
    CONTROL 
     IF, WHILE, 
     SAVE-EXCURSION 
     
    I/O 
     INTERACTIVE 
     MESSAGE string 
     PRINC expr 
     READ-FROM-MINIBUFFER 
     
     
     
    BUFFER 
     current-buffer, buffer-file-name, buffer-modified-p 
     
    POINT 
     (goto-char (point))  (point-min) (point-max) 
     
     
    FILE 
     with-temp-buffer, insert-file-contents 
     
     
    MISC 
     STRING-MATCH pattern string   (returns boolean & more)  
     sit-for, sleep-for 
