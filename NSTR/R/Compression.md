# R.Compression --


    ====================================== 
    === Compression Information Theory === 
    ====================================== 
     
     
    The shannon code for a distribution Pi(x) 
    is a list of strings lengths ceiling(-log(Pi(x))). 
    The expected length of the shannon code is 
    between H(Pi(x)) and H(Pi(x))+1 
