# aag.build --



    BUILD NOTES
    Building env on <<>> on oblio-

    - Start:MyComputer:advanced:envVars  path
      add <<>>
    - Regedit  LOCAL_MACHINE\SOFTWARE\IBM\Reading Recognition\Values  Key UserPath = <<>> Recognition\Userdata
    - TAB-S-S-C            # Run bash  
      (should try ALT-D instead)
    - cd <<>>
    - cmd.exe              # Run dos
    - cmd.bat mkall        # Builds



    NOTES:
    - Installer should compile, but "build default media" will fail since isheild is not in correct dir.
      (but mkAll should work)
