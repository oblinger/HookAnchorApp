# Reasoner --

    plans all activities of UAV during a sortie

    - output
      - 3 levels
        <<>>
         Loop:  Attend to most impt
          - if no skeleton or if skeleton fails then build one
          - if no detail plan for next step, build one
          - if executor idle, run it
          (replanning)
          - shot at then 
          - if suspicious truck then...
        <<>>
        <<>> 
        <<>>

        * <<>>
        * <<>>

    IMPL:  fwd chainer, process language, hierarchical rule sets.
