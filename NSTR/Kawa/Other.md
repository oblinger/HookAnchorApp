# Kawa.Other -- Java development environment

    - Sometimes A.java will not compile until B.java has no syntax errors 
      when F7 on A.java also compiles B.java 
     
    - REBUILD ALL not active if workspace not selected. 
     
