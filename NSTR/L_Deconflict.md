# L\_Deconflict --





    learned fly hi if shot at
    learned to check in every 20 min on truck


    Curriculum
    - Run intermediate systems and correct them
      then supply those as train/test problems
    - 


    ------
    Homework problem:
     (0) Teacher sets up conflict scenerio   (or random generator gives it)
     (1) Agent attempts it.
     (2) Expert system that checks for fixed set of error
         -> output advice

    --------
    Corrected problem:   (run once - human corrected)
     (0) Teacher sets up conflict scenerio
     (1) someone else attempts solution  (with goal dialog on)
     (2) teacher critiques goal tree and firing
         NL-goal: goal preference, precond specializaton/generalization, 
     (3) *we* apply fixed meta-reasoning strategies
         update priorities on rules

      halucinate

    -------
    Snapshot Multi-Choice
    - provide exact world state                <--- 
    - provide all factory state variables
    - provide all active goals
    - Ask:  correct goals/action

    -------
    Snapshoting opportunistic application.   (Telling live student same adivce)
    - Hand coded matching.







    imagine the goal


      
