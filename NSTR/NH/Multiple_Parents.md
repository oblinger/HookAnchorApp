# NH.Multiple\_Parents --




    DB.inst.  API for parents/children

    DB.inst.getParents(note)
    DB.inst.getChildren(note)
    DB.inst.getParent()
    DB.inst.getParents()
    DB.inst.getChildren()

    <<>> Each node has a taxonomy flag, if set 
    DB.inst.getTaxonomies()  -> Map
    DB.inst.getTaxonomy(TaxName) -> Value
