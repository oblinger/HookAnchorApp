- ! will add priority to certain entries.  and editing from the interface

- cursor positioning in iterm2
- med exercise plan weights
- nicer fonts for iterm2.  see same blogger that taught us tmux/vim
- km broken - pp sixtieth bday
%contents%





TMUXIFY
- spot --build should create anchor entries for anchor markdown pages
- spot -x should handle anchor entries and should activate those entries when called
- anchor activate should start the specified tmux session if it exists (even if it is just one window)


BEHAVIOR
CAPS - ANCHOR_NAME
- attaches or creates the named session
- 